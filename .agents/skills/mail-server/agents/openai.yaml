interface:
  display_name: "Mail Server"
  short_description: "Repo-specific patterns and workflows for mail-server"
  default_prompt: "Use the mail-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true